{
  "tools": [
    {
      "name": "get_prediction",
      "description": "Get a predicted housing metric for a given location and time.",
      "parameters": {
        "type": "object",
        "properties": {
          "location": { "type": "string" },
          "year": { "type": "number" },
          "metric": { "type": "string" }
        },
        "required": ["location", "year", "metric"]
      }
    }
  ]
}
